#include <stdio.h>

/**
* main - use printf to print a string
*
* Return: 0 (success)
*/

int main(void)
{
	char str[] = ("with proper grammar, but the outcome is a piece of art,");

	printf("%s\n", str);

	return (0);
}
